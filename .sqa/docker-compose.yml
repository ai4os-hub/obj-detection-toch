version: "3.6"

services:
  thisrepo-testing:
    image: "deephdc/ci_cd-obj_detect_pytorch"
    container_name: "${BUILD_TAG}"
    hostname: "${BUILD_TAG}"
    working_dir: /srv
    command: >
      sh -c "git config --global --add safe.directory /srv &&
             /usr/sbin/sshd -D"
    volumes:
      - type: bind
        source: ./
        target: /srv
